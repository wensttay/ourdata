CREATE TABLE DATASET(
   ID TEXT,
   AUTHOR TEXT,
   AUTHOR_EMAIL TEXT,
   CREATOR_USER_ID TEXT,
   LICENSED_ID TEXT,
   LICENSED_TITLE TEXT,
   LICENSED_URL TEXT,
   MAINTAINER TEXT,
   MAINTAINER_EMAIL TEXT,
   METADATA_CREATED TIMESTAMP,
   METADATA_MODIFIED TIMESTAMP,
   NAME TEXT,
   NOTES TEXT,
   NOTES_RENDERED TEXT,
   NUM_RESOURCES INT,
   NUM_TAGS INT,
   OWNER_ORG TEXT,
   REVISION_ID TEXT, 
   REVISION_TIMESTAMP TIMESTAMP,
   STATE TEXT,
   TITLE TEXT,
   TYPE TEXT,
   URL TEXT,
   VERSION TEXT,
   IS_OPEN BOOLEAN,
   IS_PRIV BOOLEAN,
   PRIMARY KEY(ID)
);

CREATE TABLE RESOURCE(
   ID TEXT,
   CACHE_LAST_UPDATED TEXT,
   CACHE_URL TEXT,
   CREATED TIMESTAMP,
   DESCRIPTION TEXT,
   FORMAT TEXT,
   HASH TEXT,
   LAST_MODIFIED TEXT,
   MIMETYPE TEXT,
   MIMETYPE_INNER TEXT,
   NAME TEXT,
   OWNER TEXT,
   ID_DATASET TEXT,
   POSITION INT,
   RESOURCE_GROUP_ID TEXT,
   RESOURCE_TYPE TEXT,
   REVISION_ID TEXT,
   REVISION_TIMESTAMP TEXT,
   SIZE TEXT,
   STATE TEXT,
   URL TEXT,
   URL_TYPE TEXT,
   WEBSTORE_LAST_UPDATED TIMESTAMP,
   WEBSTORE_URL TEXT,
   PRIMARY KEY(ID)
);

CREATE TABLE TAG(
   ID TEXT,
   DISPLAY_NAME TEXT,
   NAME TEXT,
   REVISION_TIMESTAMP TIMESTAMP,
   STATE TEXT,
   VOCABULARY_ID TEXT,
   PRIMARY KEY(ID)
);

CREATE TABLE DATASET_TAG(
   ID_DATASET TEXT,
   ID_TAG TEXT,
   PRIMARY KEY(ID_DATASET, ID_TAG)
);

CREATE TABLE DATASET_RESOURCE(
   ID_DATASET TEXT,
   ID_RESOURCE TEXT,
   PRIMARY KEY(ID_DATASET, ID_RESOURCE)
);


CREATE TABLE DATASET_OTHER(
   KEY TEXT,
   VALUE TEXT,
   ID_DATASET TEXT,
   PRIMARY KEY(KEY, ID_DATASET)
);

CREATE TABLE RESOURCE_OTHER(
   KEY TEXT,
   VALUE TEXT,
   ID_RESOURCE TEXT,
   PRIMARY KEY(KEY, ID_RESOURCE)
);

CREATE TABLE ACTIVITY(
   ID TEXT,
   APPROVED_TIMESTAMP TIMESTAMP,
   AUTHOR TEXT,
   MESSAGE TEXT,
   STATE TEXT,
   TIMESTAMP TIMESTAMP,
   PRIMARY KEY(ID)
);

CREATE TABLE ORGANIZATION(
   ID TEXT,
   APPROVAL_STATUS TEXT,
   CREATED TIMESTAMP,
   DESCRIPTION TEXT,
   DISPLAY_NAME TEXT,
   IMAGE_DISPLAY_URL TEXT,
   IMAGE_URL TEXT,
   NAME TEXT,
   NUM_FOLLOWERS INT,
   DATASET_COUNT INT,
   REVISION_ID TEXT,
   STATE TEXT,
   TITLE TEXT,
   TYPE TEXT,
   IS_ORGANIZATION BOOLEAN,
   PRIMARY KEY(ID)
);

CREATE TABLE DATASET_RELATIONSHIP_AS_OBJECT(
   ID TEXT,
   COMMENT TEXT,
   OBJECT TEXT,
   SUBJECT TEXT,
   TYPE TEXT,
   PRIMARY KEY(ID)
);

CREATE TABLE DATASET_RELATIONSHIP_AS_SUBJECT(
   ID TEXT,
   COMMENT TEXT,
   OBJECT TEXT,
   SUBJECT TEXT,
   TYPE TEXT,
   PRIMARY KEY(ID)
);

CREATE TABLE DATASET_DATASET_RELATIONSHIP_AS_OBJECT(
   ID_DATASET_RELATIONSHIP_AS_OBJECT TEXT,
   ID_DATASET TEXT,
   PRIMARY KEY(ID_DATASET_RELATIONSHIP_AS_OBJECT, ID_DATASET)
);

CREATE TABLE DATASET_DATASET_RELATIONSHIP_AS_SUBJECT(
   ID_DATASET_RELATIONSHIP_AS_SUBJECT TEXT,
   ID_DATASET TEXT,
   PRIMARY KEY(ID_DATASET_RELATIONSHIP_AS_SUBJECT, ID_DATASET)
);

CREATE TABLE DATASET_ORGANIZATION(
   ID_ORGANIZATION TEXT,
   ID_DATASET TEXT,
   PRIMARY KEY(ID_ORGANIZATION, ID_DATASET)
);

CREATE TABLE GRUPO(
   ID TEXT,
   APPROVAL_STATUS TEXT,
   CREATED TIMESTAMP,
   DESCRIPTION TEXT,
   DISPLAY_NAME TEXT,
   IMAGE_DISPLAY_URL TEXT,
   IMAGE_URL TEXT,
   NAME TEXT,
   NUM_FOLLOWERS INT,
   DATASET_COUNT INT,
   REVISION_ID TEXT,
   STATE TEXT,
   TITLE TEXT,
   TYPE TEXT,
   IS_ORGANIZATION BOOLEAN,
   PRIMARY KEY(ID)
);

CREATE TABLE DATASET_EXTRA(
   KEY TEXT,
   VALUE TEXT,
   ID_DATASET TEXT,
   PRIMARY KEY(KEY, ID_DATASET)
);

CREATE TABLE ORGANIZATION_EXTRA(
   KEY TEXT,
   VALUE TEXT,
   ID_ORGANIZATION TEXT,
   PRIMARY KEY(KEY, ID_ORGANIZATION)
);

CREATE TABLE GRUPO_EXTRA(
   KEY TEXT,
   VALUE TEXT,
   ID_GRUPO TEXT,
   PRIMARY KEY(KEY, ID_GRUPO)
);


CREATE TABLE DATASET_GRUPO(
   ID_DATASET TEXT,
   ID_GRUPO TEXT,
   PRIMARY KEY(ID_DATASET, ID_GRUPO)
);

CREATE TABLE GRUPO_GRUPOS(
   ID_GRUPO_SON TEXT,
   ID_GRUPO_PARENT TEXT,
   PRIMARY KEY(ID_GRUPO_SON, ID_GRUPO_PARENT)
   
);

CREATE TABLE ORGANIZATION_GRUPO(
   ID_ORGANIZATION TEXT,
   ID_GRUPO TEXT,
   PRIMARY KEY(ID_ORGANIZATION, ID_GRUPO)
);



CREATE TABLE DATASET_TRACKING_SUMMARY(
   RECENTE INT,
   TOTAL INT,
   ID_DATASET TEXT,
   PRIMARY KEY(ID_DATASET)
);

CREATE TABLE RESOURCE_TRACKING_SUMMARY(
   RECENTE INT,
   TOTAL INT,
   ID_RESOURCE TEXT,
   PRIMARY KEY(ID_RESOURCE)
);

CREATE TABLE USERS(
   ID TEXT,
   ABOUT TEXT,
   CAPACITY TEXT,
   CREATED TIMESTAMP,
   DISPLAY_NAME TEXT,
   EMAIL TEXT,
   EMAIL_HASH TEXT,
   FULL_NAME TEXT,
   NAME TEXT,
   NUM_FOLLOWERS INT,
   NUMBER_ADMINISTERED_PACKAGES INT,
   NUMBER_OF_EDITS INT,
   OPEN_ID TEXT,
   PASSWORD TEXT,
   STATE TEXT,
   IS_ACTIVITY_STREAMS_EMAIL_NOTIFICATIONS BOOLEAN,
   IS_SYS_ADMIN BOOLEAN,
   PRIMARY KEY(ID)
);

CREATE TABLE GRUPO_USERS(
   ID_USER TEXT,
   ID_GRUPO TEXT,
   PRIMARY KEY(ID_USER, ID_GRUPO)
);

CREATE TABLE ORGANIZATION_USERS(
   ID_USER TEXT,
   ID_ORGANIZATION TEXT,
   PRIMARY KEY(ID_USER, ID_ORGANIZATION)
);

CREATE TABLE ACTIVITY_GRUPO(
   ID_ACTIVITY TEXT,
   ID_GRUPO TEXT,
   PRIMARY KEY(ID_ACTIVITY, ID_GRUPO)
);

CREATE TABLE ACTIVITY_DATASET(
   ID_DATASET TEXT,
   ID_ACTIVITY TEXT,
   PRIMARY KEY(ID_DATASET, ID_ACTIVITY)
);

CREATE TABLE USERS_ACTIVITY(
   ID_USER TEXT,
   ID_ACTIVITY TEXT,
   PRIMARY KEY(ID_USER, ID_ACTIVITY)
);